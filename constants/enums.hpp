#pragma once

enum COMMAND_RESULT
{
	EMPTY,
	VALID,
	INVALID,
	NOT_CONNECTED
};

enum COMMAND_OPERATION
{
	INCREASE,
	DECREASE,
	TURN_ON,
	TURN_OFF,
	SET_DEFAULT,
	SET_SETPOINT
};

enum THERMOSTAT_STATUS
{
	OFF,
	ON,
};